{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch KafkaLens",
            "type": "node-terminal",
            "request": "launch",
            "preLaunchTask": "build",
            "command": "dotnet run --project AvaloniaApp/AvaloniaApp.Desktop/AvaloniaApp.Desktop.csproj --no-build",
            "cwd": "${workspaceFolder}"
        }
    ]
}