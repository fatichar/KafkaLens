<UserControl x:Class="KafkaLens.App.Controls.Navigator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             d:DesignHeight="980" d:DesignWidth="330"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:KafkaLens.App.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:OpenedClusterViewModel}">
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType = "{x:Type viewmodels:TopicViewModel}"
                                ItemsSource = "{Binding Path=Partitions}">
            <TextBlock Text="{Binding Path=Name}"/>
        </HierarchicalDataTemplate>

        <DataTemplate DataType = "{x:Type viewmodels:PartitionViewModel}">
            <TextBlock Text="{Binding Path=Name}"/>
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel 
            Height="Auto"
            VerticalAlignment="Stretch">
        <Label DockPanel.Dock="Top" Content="{Binding Name}"/>
        <TreeView x:Name="topicsTree" 
                  ItemsSource="{Binding Topics}" 
                  SelectedItemChanged="TreeView_SelectedItemChanged"
                  Height="Auto"
                  VerticalAlignment="Stretch"
                  >
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsSelected" Value="{Binding IsSelected}" />
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </StackPanel>
</UserControl>
